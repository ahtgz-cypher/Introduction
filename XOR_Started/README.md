# Challenge: Bài cho một chuỗi "label" và yêu cầu ta xor chuỗi đó với 13
# Solution: Viết hàm thực hiện:
string = "label"

Flag = "".join([chr(ord(c) ^ 13) for c in string]

print(Flag)

crypto{aloha}
